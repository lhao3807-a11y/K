# 注意事项

1. 每次改动完成后，都必须创建一个对应的 Git commit，以便后续追踪和回滚；
2. 每次改动后，都必须编写或更新相关测试，并在交付给用户前，确保所有测试和验证全部通过。
3. 版本号规范：当前版本 1.1.0（程序名 DynastyKline—V1.1.0）。小改动（文案/样式/局部交互微调）递增第三位，如 1.0.1；大改动（新功能/布局重构/曲线算法变更）递增第二位，如 1.1.0。发版时同步更新：app.py 的 APP_VERSION、build.bat 与 DynastyKline.spec 的产物名（DynastyKline-V{版本}）、两页页脚。
